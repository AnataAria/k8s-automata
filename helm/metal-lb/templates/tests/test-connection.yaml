apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metal-lb.fullname" . }}-test-connection"
  labels:
    {{- include "metal-lb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metal-lb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
