apiVersion: v1
kind: Service
metadata:
  name: {{ include "metal-lb.fullname" . }}
  labels:
    {{- include "metal-lb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metal-lb.selectorLabels" . | nindent 4 }}
