---
- name: Install etcd binaries and dependencies
  ansible.builtin.import_tasks: install.yml
  tags:
    - etcd
    - install

- name: Configure etcd service and system settings
  ansible.builtin.import_tasks: config.yml
  tags:
    - etcd
    - config

- name: Start and manage etcd service
  ansible.builtin.import_tasks: service.yml
  tags:
    - etcd
    - service

- name: Setup maintenance and monitoring scripts
  ansible.builtin.import_tasks: maintenance.yml
  tags:
    - etcd
    - maintenance

- name: Verify etcd cluster health
  ansible.builtin.import_tasks: verify.yml
  tags:
    - etcd
    - verify

- name: Cleanup temporary files
  ansible.builtin.import_tasks: cleanup.yml
  tags:
    - etcd
    - cleanup
