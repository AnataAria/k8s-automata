---
- name: Prepare master node environment
  ansible.builtin.import_tasks: 00-prepare.yml
  tags:
    - k8s-master
    - prepare

- name: Setup and verify certificates
  ansible.builtin.import_tasks: 01-certificates.yml
  tags:
    - k8s-master
    - certificates

- name: Generate configuration files
  ansible.builtin.import_tasks: 02-config.yml
  tags:
    - k8s-master
    - config

- name: Initialize Kubernetes cluster
  ansible.builtin.import_tasks: 03-init.yml
  tags:
    - k8s-master
    - init



- name: Generate join tokens
  ansible.builtin.import_tasks: 05-tokens.yml
  tags:
    - k8s-master
    - tokens

- name: Save join tokens to files
  ansible.builtin.import_tasks: 06-save-join-tokens.yml
  tags:
    - k8s-master
    - join

- name: Join additional masters
  ansible.builtin.import_tasks: 07-join-masters.yml
  tags:
    - k8s-master
    - join

- name: Setup kubectl access
  ansible.builtin.import_tasks: 04-kubectl.yml
  tags:
    - k8s-master
    - kubectl

- name: Configure node settings
  ansible.builtin.import_tasks: 08-node-config.yml
  tags:
    - k8s-master
    - node-config

- name: Configure backup and logging
  ansible.builtin.import_tasks: 09-backup-logging.yml
  tags:
    - k8s-master
    - backup
    - logging

- name: Verify cluster
  ansible.builtin.import_tasks: 10-verify.yml
  tags:
    - k8s-master
    - verify
